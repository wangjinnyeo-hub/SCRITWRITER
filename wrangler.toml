# Cloudflare Pages — 저장소 루트에서 npx wrangler deploy 실행 시 사용
# (빌드 결과물은 script-writer-web/dist 에 있음)
name = "script-writer-web"
compatibility_date = "2024-01-01"

[assets]
directory = "./script-writer-web/dist"
